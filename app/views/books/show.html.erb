<%= cl_image_tag @book.cover %>
<h1><%= @book.title %></h1>
<p>by <%= @book.author %></p>
<p><%= @book.description %></p>
<%= link_to "borrow", book_events_path(book_id: @book.id), method: :post, class: "btn btn-primary btn-lg" %>
<%= link_to "back to library", books_path, class: "btn btn-primary btn-lg" %>
